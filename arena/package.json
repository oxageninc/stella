{
  "name": "stella-arena",
  "version": "0.1.0",
  "private": true,
  "description": "Head-to-head coding-agent benchmark harness: run agents on receipted tasks, compare pass rate / speed / cost, and browse results in a local web dashboard. Zero runtime dependencies.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "cli": "node src/cli.mjs",
    "dev": "node src/cli.mjs dev",
    "smoke": "node src/cli.mjs run --agents mock-solver,mock-noop --trials 1"
  }
}
